BlankDelay Desktop v2.0 — NEW REDESIGNED APPS
==============================================

This is the updated build with the new splash screen, key activation,
Controller Macro overlay, Zero Delay toggle grid, and Premium dashboard.

DOWNLOAD (direct link after GitHub release upload):
  https://github.com/qboubert-maker/Cursor/releases/download/blankdelay-desktop-v2/BlankDelay-Setup.exe

HOW TO RUN
----------
1. Download BlankDelay-Setup.exe (~76 MB)
2. If Windows warns you: More info → Run anyway
3. Double-click BlankDelay-Setup.exe — it launches the Product Hub (no separate install step)
4. Click any product (e.g. Controller Macro V2)
5. Tap the splash screen → enter license key → use the app

TEST KEY (any valid format works):
  BD-DEMO-2025-TEST

WHAT'S NEW IN v2
----------------
• Splash screen: BlankDelay logo + product name + tap to continue
• Key activation screen before the app opens
• Zero Delay / Zero Delay Plus: blue toggle grid (core latency, keyboard/mouse, debloat)
• Controller Macro: PS5/Xbox controller preview + macro toggles
• Premium Utility: dashboard layout with sections and stats
• All products use BlankDelay branding (not Aphrodite/SJ)

NOTE: This v2 build is a portable Windows app (.exe). It runs directly when you
double-click it — you do NOT need the old installer from before Aug 2026.
