[package]
name = "tap_length"
version = "1.0.0"
edition = "2021"
description = "High-resolution per-button tap length floor for Blank Optimizer"

[lib]
name = "tap_length"
crate-type = ["cdylib"]

[profile.release]
lto = true
codegen-units = 1
opt-level = 3
strip = true
